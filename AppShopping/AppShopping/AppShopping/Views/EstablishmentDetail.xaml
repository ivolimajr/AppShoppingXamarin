<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModel="clr-namespace:AppShopping.ViewModels" 
             xmlns:partial="clr-namespace:AppShopping.Views.Partials"
             x:Class="AppShopping.Views.EstablishmentDetail">
    
    <ContentPage.BindingContext>
        <ViewModel:EstablishmentDetailViewModel/>
    </ContentPage.BindingContext>

    <Shell.TitleView>
        <partial:Header />
    </Shell.TitleView>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Restaurante"/>
            <Label Text="{Binding Establishment.Name}" HorizontalOptions="Center" VerticalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>